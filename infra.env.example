# Shared infra credentials — used by docker-compose only
# Copy to infra.env and fill in real values. Never commit infra.env.

POSTGRES_DB=expenses
POSTGRES_USER=tikinti_user
POSTGRES_PASSWORD=changeme_db_password

RABBITMQ_DEFAULT_USER=tikinti_rabbit
RABBITMQ_DEFAULT_PASS=changeme_rabbit_password
