# Frontend config — public-safe only
# No secrets, no API keys, no JWT secrets ever in this file.
# Copy to frontend/.env and update for your environment.

NUXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1
